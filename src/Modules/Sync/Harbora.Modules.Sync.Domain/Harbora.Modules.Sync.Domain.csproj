<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Entities and their invariants. Sits below Harbora.Data, which references it for DbSets and the
    migration, so it must never reference Data back.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\Harbora.Domain\Harbora.Domain.csproj" />
    <ProjectReference Include="..\Harbora.Modules.Sync.Contracts\Harbora.Modules.Sync.Contracts.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Harbora.Modules.Sync.Domain</RootNamespace>
  </PropertyGroup>

</Project>
